# Deployment Guide

**Audience:** whoever hosts the site.

**Status:** placeholder. Filled in as the corresponding work is delivered.

Production hosting, environment variables, backups, and how to deploy a new version. The prior team's deployment ran on a GCP VM that no longer exists; our hosting decision is tracked in the backlog.
